const { Service } = require("feathers-sequelize");

exports.UserPayments = class UserPayments extends Service {
  // eslint-disable-next-line no-unused-vars
  constructor(options, app) {
    super(options)
  }
};
